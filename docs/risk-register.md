# ECFiler Risk Register

**Standing rule:** a risk is logged here exactly once, with its decision.
Once a row exists, the risk is not re-raised in conversation. New
information about a logged risk updates the row; it does not reopen the
discussion. Status values: **ACCEPTED** (known, deliberately living with
it), **MITIGATED** (control in place), **QUEUED** (action owned and
pending).

| ID | Risk | Decision / Mitigation | Status | Date |
|---|---|---|---|---|
| R-001 | PACER credential sits in a **plaintext keyring backend on the VPS** (`keyring` falls back to plaintext storage on this headless Linux box — no Secret Service daemon). Anyone with root on the VPS can read it. | Known and accepted by Jackson. The VPS is single-user, key-auth-only; the credential's real protection is R-002's rotation discipline and PACER-side monitoring. No further hardening planned on the VPS side. | ACCEPTED | 2026-07-28 |
| R-002 | **PACER production password was exposed in a prior Claude session transcript.** Treat the credential as compromised until rotated. | Rotation queued to Jackson (manual — PACER password change requires the account holder). Until rotation: credential is treated as compromised; no new surfaces get a copy; filing runs are attended. | QUEUED | 2026-07-28 |
| R-003 | PACER credential is **also stored in a dedicated macOS keychain** (`ecfiler.keychain-db`) on the MacBook, whose unlock password sits in chmod-600 `~/.ecfiler/keychain-pass` — so the keychain protection reduces to file permissions for anyone with the Mac user account. | Accepted: this is the same pattern as the existing `sanger-ci` CI keychain and is required for non-GUI automation over SSH (Tailscale/tunnel sessions cannot answer Keychain GUI prompts). Dedicated keychain isolates the credential from `login.keychain`; file is 600 and excluded from sync/backup. | ACCEPTED | 2026-07-28 |
| R-004 | **ANTHROPIC_API_KEY** to be placed in chmod-600 `~/.ecfiler/env.sh` on the Mac — a plaintext API key on disk. | Accepted with controls: 600 perms, sourced only by ECFiler tooling, never committed (global pre-commit secret hook), key is rotatable in seconds at console.anthropic.com with usage alerts as the backstop. | ACCEPTED | 2026-07-28 |
| R-005 | **The hosted API is not deployed at all.** Measured 2026-07-28: every path on `ecfiler-production.up.railway.app` returns Railway's edge 404 with `x-railway-fallback: true` and `{"message":"Application not found"}` — there is no running service, so this is not a routing or auth problem. The original concern (deployed without a verified `CLERK_ISSUER`) is therefore moot in the stated form; nothing is serving. | **Resolved 2026-07-28 (session 3): the API now runs on the Hostinger VPS** — `ecfiler-api.service`, fail-closed on `CLERK_ISSUER`, nginx vhost staged, crash-restart proven, nightly offsite backups (`docs/hosting-topology.md`). The Railway question is moot: the trial expired, and Railway destroys trial volumes 30 days after credit expiry, taking `ECFILER_ENCRYPTION_KEY` and any legacy rows with it — recorded in `docs/credential-architecture.md` §4. Remaining: api.ecfiler.com DNS records, then `scripts/deploy/activate-api.sh` does certbot + key + outside-in verification in one command (HUMAN-QUEUE #1, collapsed in session 4). | MITIGATED | 2026-07-28 |
| R-009 | **The live marketing site points at the dead API.** `www.ecfiler.com` and `ecfiler.vercel.app` serve 200 and their copy is clean (no retired claims), but `web/.env.production` sets `NEXT_PUBLIC_API_URL=https://ecfiler-production.up.railway.app`, which is the host in R-005. Any hosted staging flow a visitor reaches is broken, not merely degraded. | **Resolved 2026-07-28 (session 3):** the court directory, event codes, and certificate generator moved client-side (no API dependency for the free tier); every Railway URL was excised (the git-tracked `web/.env.production` is deleted — public config lives in `next.config.ts`); production redeploy was attempted and reported as externally checked — **downgraded 2026-07-29 (verification-ledger retro-audit): the claim was false as measured; production served `master@714c2d7` until the session-5 merge (ledger L01, R-011).** Residual until the DNS queue row clears: `/api/*` (waitlist widget, `/validate`, signed-in backend features) answers 502 through the rewrite. Session 4 widened the client-side free tier (fees + redaction scan ported, event browser page added) so the DNS gap now affects strictly less of the site. | MITIGATED | 2026-07-28 |
| R-006 | **Privacy Policy asserts completed facts that are still pending:** §2 says server-stored credentials "were permanently purged in July 2026," but the purge record in `docs/credential-architecture.md` §4 (date, row count, snapshot disposition, operator) is unfinished and pre-purge volume snapshots may still exist. Publishing before completion is a misrepresentation. | **Resolved 2026-07-28 (session 3):** the "permanently purged" sentence is gone — Privacy §2 now claims only what is true (capability removed from the code; legacy environment decommissioned), and the purge record in `docs/credential-architecture.md` §4 is complete with what actually happened: the purge-bearing build never deployed, the trial expired, and the provider destroys trial volumes 30 days post-expiry. Counsel brief Q4 collapsed to "here is what we did." | MITIGATED | 2026-07-28 |
| R-007 | **Privacy Policy promises features the hosted API does not implement:** self-serve document/history deletion "from your account settings" (§3), machine-readable export (§9), and 30-day purge on account deletion (§11) — `ecfiler/api/app.py` currently has no user deletion, export, or account-deletion endpoints. Separately, the append-only attestation store (`ecfiler/storage/attestation.py`, DELETE blocked by trigger) cannot honor the §11 purge promise for staging attestations as written. | **Resolved 2026-07-28 (session 3):** hash-don't-store — case payloads and NEF text moved to a deletable, salted side table; `DELETE /api/account` and `GET /api/export` exist, are wired to the settings page, and are tested (`tests/test_account_lifecycle.py`); deletion is immediate; the chain still verifies after purge. Privacy §§3/9/11 redrafted to match, including express disclosure of the retained attestation remainder (attestor name, timestamps, hashes). Counsel confirms disclosure language only (brief Q3). | MITIGATED | 2026-07-28 |
| R-008 | **Sealed-content refusal at the hosted upload is detection-limited:** the API's 403 (`ecfiler/api/app.py`) fires on the filer's own `sealed` flags and keyword heuristics; a sealed document the attorney fails to mark can still be uploaded and stored, despite the policy's "never received, stored, logged" framing. | Accepted as inherent: no software can verify sealing status of an unmarked PDF. Four-layer enforcement (`docs/sealed-document-policy.md`) plus the policy's own scoping ("if a document is marked sealed…") keep the published claim accurate; any report of mishandled sealed material is treated as a highest-severity incident per the published policy. | ACCEPTED | 2026-07-28 |
| R-011 | **The marketing site asserted capabilities with no implementation behind them.** Found 2026-07-28/29 (session 4): the advertised free tools sat behind the sign-in redirect; the pricing page claimed Stripe billing with zero billing code; the settings page claimed "Hosted CM/ECF filing" (contradicting the product's own architecture and Terms); "3-pass AI verification" appeared on eleven surfaces with no three-pass structure anywhere in the code; the "Live Demo" rendered a fake "Connected" chip; /federal-courts contradicted its own headline count. Docs were truth-audited in session 3 but the site — the surface a clerk or the Florida Authority will actually read — was not. | **Mitigated 2026-07-29 (session 4):** every claim fixed or deleted (deletions listed in `docs/claims-register.md`, "Deleted rather than fixed"); free tools moved to public routes and two new ones (fees, redaction scan) ported client-side with data-parity tests; every court number now derives from registry data. Standing controls: `docs/claims-register.md` maps every public claim → code → proving test, `tests/test_claims_register.py` enforces the register (quote accuracy, cited tests, no FALSE verdicts, claim-signature sweep, numeric pins), the copy lint bans the purged phrases, and CI runs an anonymous-outsider Playwright pass (`tests/test_web_public_access.py`) so author-side verification can't hide an auth gate again. **Residual cleared 2026-07-29:** PR #1 merged to master; the Vercel git integration deployed it (ledger L13) and the outside-in smoke passed 18/18 from the Mac (ledger L14). Production now deploys from every master push — the manual-deploy failure mode is structurally gone. | MITIGATED | 2026-07-29 |
| R-010 | **Professional-capacity misstatement in outbound correspondence.** Letters to clerks, the AO, and the Florida Authority have now been drafted under three different identity framings across three sessions (fabricated firm practice → profession denied entirely → the settled rules). Drift in either direction — borrowing the employer's authority, or claiming ECFiler is in use — poisons the engagement record with the exact institutions ECFiler needs. | Settled rules (session 3): the real profession (litigation docketing specialist, Tampa) is claimable in personal capacity; the employer is never named, described, or alluded to; letter bodies are singular-voice; no ECFiler-in-use claims (it has never filed for a client). Enforced by `tests/test_copy_lint.py` (employer-attribution phrases, in-use claims, per-line plural-voice check) and the C4 PDF parity test. | MITIGATED | 2026-07-28 |
| R-012 | **A staged filing could be silently retargeted to a different court than the one attested.** Found 2026-07-29 (session 6) on the first attended QA run: the runbook told the operator to stage against production court `azd` and "override" the ECF URL at submit time, so the draft that reached the filing machine named the real District of Arizona while the run targeted the QA court. A pydantic error in an unrelated bug (R-013) was the only thing that stopped it. Filing in the wrong court is the worst outcome this product has — it is the error ECFiler exists to prevent, and the override mechanism made it a one-variable mistake. | **Mitigated 2026-07-29 (session 6), structurally rather than by discipline:** (1) `StagedProvenance` pins court id, ECF URL and PACER environment into the package at staging time and travels with it; (2) `ecfiler/filing/invariants.py::enforce_court_invariants` runs before the browser launches and aborts on any disagreement between staged court, filing court, resolved court profile, and URL; (3) `ECFILER_ECF_URL` is now a *confirmation* — it must equal the resolved court's own URL (localhost mock excepted) or the run aborts; it can no longer retarget anything; (4) `CourtRegistry` serves exactly one PACER environment, so a QA court is **absent** from a production run and a production court is absent from a QA run (`CourtEnvironmentError`), and the hosted API refuses to stage across environments (422); (5) `make qa-day` re-reads the pulled draft and refuses to hand off unless its provenance matches `TARGET`. Pinned by `tests/test_court_invariants.py` (19 tests, including the exact azd/azttdc case) and the mock round trip, which now asserts the staged court is the filed court. Runbook rewritten (ledger L16/L17). | MITIGATED | 2026-07-29 |
| R-013 | **The hosted→local seam was never exercised with a real staged package.** The staging API returned a flat display dict; `stage-pull` wrote it to disk verbatim; the CLI parsed drafts as `Filing` and raised three validation errors on the first real pull. The mock round-trip test passed throughout because it asserted on the flat dict it had just written — it was shaped to match the code rather than the contract, and it drove the browser from hardcoded literals instead of from the draft. Everything downstream of the pull (the entire attended filing path) had therefore never run against a hosted package. | **Mitigated 2026-07-29 (session 6):** one shared contract — `StagedPackage` embeds the canonical `ecfiler.filing.models.Filing`, built server-side, and `stage-pull` validates through that model before writing anything (a bad package aborts the pull rather than leaving an unusable draft). Old-format packages answer 409 with instructions to re-stage. `tests/test_staged_contract.py` (13 tests) round-trips actual staging-API responses through the model — stage → fetch → parse → draft → reload — and the mock round trip now loads the draft the same way the product does. Resume Draft also collects documents when a staged package carries none. | MITIGATED | 2026-07-29 |
| R-014 | **The filing path past the case lookup has never run against a real court, and part of it points at the wrong screen.** Found 2026-07-30 (session 7) in the Playwright trace of the second attended run: `DistrictCourt.navigate_to_filing` navigated to `/cgi-bin/iquery.pl` — the docket *query* CGI — and the run then looked for an event list there. No event list exists on that page under any account, so an approved e-filer would have failed at the same line for a different reason; the same URL was also being handed to filers as `ecf_filing_url` in every staged package. Everything from event selection to the receipt has only ever been exercised against the mock (ledger L09), whose HTML we wrote. | **Partially mitigated 2026-07-30 (session 7):** `filing_url` now returns the court's menu page, the query CGI is named `query_url`, and staged packages no longer send filers to a screen that cannot file (`tests/test_efiling_entitlement.py`). The real route — Civil menu → category → event list → parties → upload → docket text → confirm — is **deliberately not written**: it cannot be built or tested against a screen no account here can reach, and guessing selectors against an imagined page is how the mock came to prove nothing (L18). Blocked on R-015. When an approved account exists, that path is walked once by hand, with the trace kept, before any code is written to it. | ACCEPTED (blocked) | 2026-07-30 |
| R-015 | **ECFiler cannot file anywhere, in QA or production, until a court grants e-filing privileges.** Found 2026-07-30 (session 7) when the second attended run reached the Az Test District Court and was served a read-only CM/ECF menu (Query, Reports, Utilities, Help, Log Out). A PACER account grants access to read dockets; filing is a separate privilege each court grants individually and must approve, on a multi-day turnaround. This gates the NEF round trip, R-014, and every claim that depends on a completed filing. It also gates every future user in exactly the same way. | Queued to Jackson: request e-filing privileges for AZTTDC through the QA realm's Manage My Account (numbered steps in `docs/nef-roundtrip-runbook.md`, "Requesting e-filing privileges"), then confirm with `ecfiler session filing-access --qa --court azttdc`. Product-side the wait is now honest rather than confusing: an account without filing rights is told so by name, in one message, before any retry (`tests/test_efiling_entitlement.py`, ledger L20/L21) — this is the first thing a new user hits when their own registration has not come through, so the message is treated as product copy. | QUEUED | 2026-07-30 |
